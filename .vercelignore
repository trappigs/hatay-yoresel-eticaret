# The Medusa backend is deployed separately (VPS/Railway/Render), not on Vercel.
backend/

# Local-only
docs/
.playwright-mcp/
*.log

# never upload local env (Vercel uses dashboard env vars)
.env
.env*.local
